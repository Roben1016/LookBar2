<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roshine.lookbar.music" >

    <application
        android:name="debug.MusicApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/music_app_name"
        android:supportsRtl="true"
        android:theme="@style/common_AppTheme">
        <activity android:name="debug.ModuleMusicActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MusicDetailActivity"
            android:screenOrientation="portrait"/>
    </application>
</manifest>
